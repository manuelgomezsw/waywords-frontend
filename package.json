{
  "name": "waywords-frontend",
  "version": "0.1.0",
  "description": "Angular 19 frontend for Waywords",
  "scripts": {
    "start": "echo 'Dev server will be implemented in development'",
    "build": "echo 'Building...' && mkdir -p dist && touch dist/index.html",
    "build:prod": "echo 'Building production...' && mkdir -p dist && touch dist/index.html",
    "test": "echo 'Tests will be implemented in development'",
    "lint": "echo 'ESLint will be configured in development'",
    "format:check": "echo 'Prettier will be configured in development'"
  },
  "private": true,
  "dependencies": {
    "@angular/core": "^19.0.0",
    "rxjs": "~7.8.0",
    "tslib": "^2.3.0",
    "zone.js": "~0.15.0"
  },
  "devDependencies": {
    "typescript": "~5.5.0"
  }
}
